// todo

Security
  var arr = [];
  for(var i=8747; i<8756; arr.push(i++));
  arr.push(255);
  for(var i=10764; i<10781; arr.push(i++));
  new TextDecoder('utf-16').decode(new Uint16Array(arr));

Icon
  import javax.imageio.ImageIO;
  import java.io.File;
  setIconImage(ImageIO.read(new File("Icon.png")));

Username storage/rejection
  DMs, :message

Checkboxes
  Trump bot
  HTML/Markdown (http://daringfireball.net/projects/markdown/syntax)
